# Allows the user to populate the world at the beginning of a simulation

Allows the user to populate the world at the beginning of a simulation

## Usage

``` r
apply_modifiers(space, modifiers)
```

## Arguments

- space:

  the space of the current time-step

- modifiers:

  the modifier object created by the user

## Value

must return the modified "space\$environment".
